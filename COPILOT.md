# GitHub Copilot Configuration

Follow these styling rules:
- Code style: Use modern TypeScript with ES6 features.
- Logging: Use structured logging, do not use `console.log` in production-facing API handlers.
